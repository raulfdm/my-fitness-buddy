import 'package:flutter/material.dart';

class DietSheetsScreen extends StatelessWidget {
  static String route = '/diet';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Diet'),
    );
  }
}
